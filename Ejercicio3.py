# Hacer un programa que pida una palabra y la muestre al reves.
# Usando el operador [::-1]

palabra = input('Ingrese la frase : ')
invertida = palabra[::-1]
print(f'La palabra invertida es : {invertida}')
